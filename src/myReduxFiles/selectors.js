export const cartSelector = (state) => state.cartData;

export const countryStateCitySelector = (state) => state.countryStateCity;

export const guestDataSelector = (state) => state.guestData;

export const emailSelector = (state) => state.email;

export const userLoadingSelector = (state) => state.userLoading;
